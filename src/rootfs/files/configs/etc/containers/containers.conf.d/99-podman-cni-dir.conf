# Podman always uses an isolated CNI config directory so kubelet (RKE2/Canal)
# never competes with or falls back to the Podman bridge plugin.
[network]
network_config_dir = "/etc/cni/podman/net.d"
default_network = "podman"
