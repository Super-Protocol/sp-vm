#!/bin/bash
set -euo pipefail

# Run the bundled schema-sync CLI for swarm-cloud-api from the shared
# /opt/swarm-cloud-api monorepo dist directory.
cd /opt/swarm-cloud-api
exec node ./apps/swarm-cloud-api/dist/cli/schema-sync.js "$@"
