#!/bin/bash
set -euo pipefail

# Backwards-compatible wrapper: delegate to the canonical schema-sync
# script located in /usr/local/lib/swarm-cloud, which is what the
# provision plugin expects (API_INSTALL_DIR / "schema-sync").
exec /usr/local/lib/swarm-cloud/schema-sync "$@"
