#!/bin/bash
set -euo pipefail

# Run the bundled schema-sync CLI for swarm-cloud-api from the shared
# /usr/local/lib/swarm-cloud monorepo dist directory.
cd /usr/local/lib/swarm-cloud
exec node ./dist/apps/swarm-cloud-api/cli/schema-sync.js "$@"
